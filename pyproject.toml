[project]
name = "s1trader"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Garam Lee", email = "agerio100@naver.com" }
]
requires-python = ">=3.13"
dependencies = [
    "httpx>=0.28.1",
    "numpy>=2.5.3",
    "pandas>=3.0.6",
]

[project.scripts]
s1trader = "s1trader:main"

[build-system]
requires = ["uv_build>=0.12.17,<0.13.0"]
build-backend = "uv_build"
